# Evaluation methodology

Every number here is regenerated by `make data` + `python scripts/checkpoint1.py` (Phase 1) and
`make eval` (Phases 2-3). Seed 42 throughout.

## What we claim
- Relative lift of the ML policy over Razorpay's default T+1/T+2/T+3 retry schedule on synthetic
  data calibrated to the public 55-65% raw-recovery band.
- Correct abstention under a `null_uplift` world.
- Off-policy estimates (IPS, SNIPS, DR) that track randomized A/B ground truth, with reported
  estimator error.

## What we do not claim
- Absolute rupee figures for any real merchant.
- Generalization to failure taxonomies Razorpay test mode cannot emit.

## Simulator: population and features
- 5 merchants (StreamBox Rs 299, FitPulse Rs 999, LearnLoop Rs 1,499 B2C; CloudDesk Rs 4,999,
  ScaleOps Rs 15,000 per seat B2B), 8,000 customers, 50,000 failed payments over an 18-month window.
- Customers carry hidden latent state: liquidity, engagement, churn intent, and a hidden 40%
  segment used by the `misspecified` variant. Low-liquidity customers fail more often.
- Failure mix (realised within 3 points of spec): insufficient_funds 30%, bank_technical 15%,
  auth_failed 15%, gateway_5xx 10%, card_expired 10%, risk_declined 8%, customer_cancelled 7%,
  mandate_failed 5%. `card_expired` events are always on cards; `mandate_failed` always on UPI
  Autopay / e-mandate.
- Decision-time features only: merchant, segment, amount, plan cycle, seats, failure category and
  source, instrument, bank, attempt number, time of failure (hour IST, weekday, day of month,
  days to the 1st), tenure, subscription age, 90-day failure / recovery history, last success,
  contacts in the last 24h / 7d, risk score, card expiry days. Nothing computed after the failure.

## Simulator: true outcome process
Each arm is a bounded plan (retries at fixed days, at most one reminder, optional escalation).
For a plan, recovery happens if the customer self-serves, or any retry succeeds (impossible on
a "hard" failure), or the escalation resolves; a seen reminder can also trigger cancellation.
Per-row probabilities depend on category parameters, hidden latents and interactions:

| structure | observable proxy | hidden part |
|---|---|---|
| retry success rises with delay for insufficient funds and jumps after payday | `days_to_payday` | liquidity latent scales the whole curve |
| bank_technical retries succeed only after the outage clears | none | outage duration, lognormal median 4h |
| mandate_failed needs ~24h pre-debit notice; `retry_now` fails | category | none |
| card_expired: retries useless, escalation or self-serve only | `card_expiry_days` | issuer auto-updater (3%) |
| reminder lift decays with recent contacts and can cause churn | `contacts_last_7d` | engagement, churn intent |
| repeated attempts decay (0.85 per prior attempt) | `attempt_number` | none |

All outcomes are realised with common random numbers per row (one uniform per event for the
hard-failure draw, self-serve, reminder seen, churn, escalation, and one per retry slot), so every
policy's outcome on the same row is coherent and the counterfactual table is exact.

### Calibration
A single global multiplier on retry success (`RETRY_SCALE`) is bisected so that the Razorpay
default schedule (retries at T+1, T+2, T+3, no messages) recovers 60% of failures under
`calibrated`. Result: `RETRY_SCALE = 1.3133` (`python scripts/make_data.py --calibrate`). The
same multiplier is used for the other two variants so that they differ only in hidden structure.

True mean recovery probability by category and action, `calibrated` (from the counterfactual
table; regenerate with `python scripts/checkpoint1.py --by-category`):

| category | share | no_action | retry_now | retry_delayed_1 | retry_delayed_7 | remind_and_retry | escalate_human | razorpay_default |
|---|---|---|---|---|---|---|---|---|
| insufficient_funds | 30% | 0.29 | 0.36 | 0.47 | 0.59 | 0.52 | 0.54 | 0.66 |
| bank_technical | 15% | 0.19 | 0.26 | 0.81 | 0.83 | 0.81 | 0.53 | 0.89 |
| auth_failed | 15% | 0.36 | 0.43 | 0.46 | 0.45 | 0.56 | 0.62 | 0.57 |
| gateway_5xx | 10% | 0.16 | 0.94 | 0.92 | 0.85 | 0.92 | 0.42 | 0.98 |
| card_expired | 10% | 0.23 | 0.25 | 0.25 | 0.25 | 0.33 | 0.71 | 0.25 |
| risk_declined | 8% | 0.16 | 0.19 | 0.19 | 0.19 | 0.19 | 0.47 | 0.24 |
| customer_cancelled | 7% | 0.09 | 0.12 | 0.12 | 0.12 | 0.11 | 0.28 | 0.14 |
| mandate_failed | 5% | 0.24 | 0.26 | 0.44 | 0.43 | 0.47 | 0.59 | 0.57 |

## Checkpoint 1: baselines under all three variants
Per 1,000 failures. "Wasted contacts" = reminders sent where `no_action` would also have recovered.

| variant | policy | raw recovery | Rs recovered /1k | Rs incr. vs no_action /1k | contacts /1k | wasted contacts /1k | escalations /1k |
|---|---|---|---|---|---|---|---|
| calibrated | no_action | 23.9% | 2,146,287 | 0 | 0 | 0 | 0 |
| calibrated | razorpay_default | 60.1% | 4,510,222 | 2,363,935 | 0 | 0 | 0 |
| calibrated | heuristic | 59.2% | 4,531,191 | 2,384,904 | 450 | 140 | 179 |
| misspecified | no_action | 23.1% | 2,324,649 | 0 | 0 | 0 | 0 |
| misspecified | razorpay_default | 57.7% | 4,456,504 | 2,131,855 | 0 | 0 | 0 |
| misspecified | heuristic | 60.4% | 4,661,918 | 2,337,269 | 450 | 142 | 179 |
| null_uplift | no_action | 23.9% | 2,146,287 | 0 | 0 | 0 | 0 |
| null_uplift | razorpay_default | 23.9% | 2,146,287 | 0 | 0 | 0 | 0 |
| null_uplift | heuristic | 23.7% | 2,127,730 | -18,556 | 450 | 140 | 179 |

Reading the table:
- `razorpay_default` sits at 60.1% under `calibrated` by construction and at 57.7% under
  `misspecified` without re-tuning (both inside the public band).
- The rule-table `heuristic` recovers a similar share but more rupees, because it escalates
  expired cards and risk declines (high-value B2B tickets) instead of retrying them, and it
  sends 450 reminders per 1,000 failures of which 140 are wasted.
- Under `null_uplift` every intervention is worth exactly zero and the heuristic loses money
  through reminder-induced cancellations. This is the world in which a policy must abstain.

Rupee totals are dominated by the two B2B merchants (mean ticket Rs 15.6k and Rs 46.5k versus
Rs 0.5k-2.5k for B2C), so per-merchant tables accompany every headline number from Phase 2 on.

## Logged data
The logging policy is epsilon-uniform with epsilon = 0.5 around the heuristic: every one of the
7 primitive actions has support (propensity 1/14 or 1/14 + 1/2) and the propensity is stored on
each row. Training uses only `failures.parquet`; `counterfactuals.parquet` is read by the
evaluator alone and a test forbids any reference to it from `features/` or `models/`.
